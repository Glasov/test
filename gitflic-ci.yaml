stages:
  - pre

pre:
  stage: pre
  image: maven:3.8.5-openjdk-11-slim
  scripts:
    - echo PRE
    - echo DONE!
  artifacts:
    reports:
      sast:
         - 'spotbugsSarif.json'
         - 'sast.json'
      dast:
        paths:
         - 'report.json'
