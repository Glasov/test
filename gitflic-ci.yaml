stages:
  - pre
  - dast

cache:
  paths:
    - .m2/repository/
    - core/target/
    - desktop/target/

pre:
  stage: pre
  image: maven:3.8.5-openjdk-11-slim
  scripts:
    - echo PRE
    - echo BLABLABLABLABLABLABLABLABLABLABLABLABLABLABLABLABLABLABLABLABLABLABLABLA
    - echo DONE!

dast:
  stage: dast
  image: softwaresecurityproject/zap-weekly:latest
  scripts:
    - echo DAST
    - zap.sh -addoninstall reports
    - zap.sh -h
    - echo DONE!
  artifacts:
    reports:
      paths: 'target/dast.json'
    name: hui
    paths:
      - 'pom.xml'
